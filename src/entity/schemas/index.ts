/**
 * @module @roastery/beans/entity/schemas
 *
 * Re-exports:
 * - {@link EntitySchema} — runtime `Schema` instance validating the base
 *   {@link EntityDTO} shape (homonymous with the `EntitySchema` symbol).
 */

export { EntitySchema } from "./entity.schema";
