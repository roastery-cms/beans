/**
 * @module @roastery/beans/entity/dtos
 *
 * Re-exports:
 * - {@link EntityDTO} — TypeBox schema (and matching static type) for the three
 *   universal entity fields (`id`, `createdAt`, `updatedAt?`).
 */

export { EntityDTO } from "./entity.dto";
