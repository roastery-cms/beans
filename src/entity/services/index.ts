/**
 * @module @roastery/beans/entity/services
 *
 * Re-exports:
 * - {@link ParseEntityToDTOService} — recursive entity-to-DTO flattener.
 *   Consume via {@link Mapper.toDTO} unless you specifically need the unvalidated output.
 */

export { ParseEntityToDTOService } from "./parse-entity-to-dto.service";
