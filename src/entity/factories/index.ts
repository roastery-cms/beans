/**
 * @module @roastery/beans/entity/factories
 *
 * Re-exports:
 * - {@link makeEntity} — produces a fresh `EntityDTO` (UUID + creation timestamp).
 */

export { makeEntity } from "./make-entity.factory";
